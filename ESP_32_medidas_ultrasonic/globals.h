#ifndef GLOBALS_H
#define GLOBALS_H

#include <stdint.h>

extern float threshold_cm;  // declarada como extern para acceso global
extern bool new_threshold;  // declarada flag para acceso global, cambio de valor threshold

extern bool ext_trig1;  // declarada flag para acceso global, cambio de valor threshold
extern bool ext_trig2;  // declarada flag para acceso global, cambio de valor threshold

#endif
